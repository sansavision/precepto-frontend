import { createFileRoute } from '@tanstack/react-router'
import TemplateManager from '@/app/pages/(main)/template'

export const Route = createFileRoute('/_main/template/')({
  component: TemplateManager,
})

// function RouteComponent() {
//   return <
// }
